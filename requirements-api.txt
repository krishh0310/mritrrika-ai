# apps/api — FastAPI service. Runs natively on python3.12 (see docs/architecture.md).

fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12          # multipart upload (§21)

SQLAlchemy>=2.0
alembic>=1.14
psycopg[binary]>=3.2
GeoAlchemy2>=0.15                 # PostGIS geometry columns (§11)
pgvector>=0.3                     # semantic retrieval (§10)

pydantic>=2.9
pydantic-settings>=2.5
python-dotenv>=1.0
email-validator>=2.2

# Auth (§61)
pyjwt>=2.9
argon2-cffi>=23.1

# Async pipeline
celery[redis]>=5.4
redis>=5.0
sse-starlette>=2.1                # processing progress stream (§24)

# Downloadable record certificate (§15). No reportlab: it does not shape
# Devanagari (conjuncts and matras land in the wrong places), so the
# certificate is rendered with Pillow -- which is already required to be
# libraqm-linked here -- and saved as a PDF.
qrcode>=7.4

# Object storage
boto3>=1.35

# Providers (§81)
google-genai>=1.0
groq>=0.11

# Quality assessment / preprocessing shared with the worker
opencv-python-headless>=4.10
numpy>=2.0
Pillow>=11.0
pypdfium2>=4.30          # renders uploaded PDFs to page images (upload gate + pipeline)

# Testing
pytest>=8.3
pytest-asyncio>=0.24
httpx>=0.28
# Nightly retrain check (app/tasks/retrain_scheduler.py). <4: v4 is a rewrite.
apscheduler>=3.10,<4
