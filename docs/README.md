# docs

Design and operations documentation: architecture, AI pipeline, API (`asyncapi.yaml`), data model, security, deployment, integrations (DILRMP, LRMS, GIS), and the demo walkthrough. The root README has an index.
