# scripts

Reproducible command-line entry points: seed demo data, generate synthetic documents and GIS, build and split datasets, train and evaluate models, operational helpers (API keys, GeoServer). Run them from the repo root with the project venv, for example `.venv/bin/python scripts/seed_demo.py`.
