# apps

Deployable, user-facing applications.

- `api/`: FastAPI backend (Alembic migrations, `requirements.txt`, `requirements-dev.txt`)
- `web/`: Next.js officer workstations and citizen portal
- `mobile/`: Expo citizen app and DEO field capture
