# packages

Shared code imported by apps, services and scripts.

- Python: `domain/` (canonical schema, state machine), `gis/` (cadastre generation), `dataset-generator/` (synthetic documents)
- TypeScript: `ui/` (React components, i18n), `shared-types/` (generated by `scripts/generate_shared_types.py`; do not hand-edit)
