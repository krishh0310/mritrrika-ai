# datasets

Training and evaluation data. Everything here is gitignored and regenerated by `scripts/`, except `extraction-dataset/` (small labelled JSONL splits).
See "Regenerating the dataset" in the root README.
